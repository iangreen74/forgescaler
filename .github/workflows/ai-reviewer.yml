name: AI Reviewer

on:
  pull_request:
    types: [opened, synchronize, edited]

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Analyze PR with AI
        run: |
          echo "Simulated AI review: ✅ Terraform plan looks valid and safe to apply."
