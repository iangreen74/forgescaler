name: Terraform Bootstrap

on:
  workflow_dispatch:

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Setup backend resources
        run: echo "Creating S3 bucket and DynamoDB for Terraform state..."
